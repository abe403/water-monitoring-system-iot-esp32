# Leave blank when the console is served behind its same-origin reverse proxy.
VITE_API_BASE_URL=
# Optional development-only bearer token. Production identity should inject a token at runtime.
VITE_ACCESS_TOKEN=
